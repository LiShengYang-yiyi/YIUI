using System;
using System.Collections.Generic;

namespace ${Namespace}
{
    /// <summary>
    /// Author  ${Author}
    /// Date    ${CreateDate}
    /// Desc    ${Desc}
    /// </summary>
    [FriendOf(typeof(${Name}Component))]
    public static class ${Name}ComponentSystem
    {
        #region ObjectSystem
${ObjectSystem} 
        #endregion
${Life}
    }
}